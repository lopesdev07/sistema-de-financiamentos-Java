package model;

public enum PropertyType { // Each property type can have different attributes and behaviors, which can be implemented in their respective classes.
    HOUSE,
    APARTMENT,
    LAND
}
